{
  "name": "decklee",
  "version": "0.1.0",
  "description": "DeckLee CLI — build, validate, and scaffold validated presentation decks as JSON; generates the AI-assistant contract (AGENTS.md + skills).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "decklee": "./dist/bin.js"
  },
  "main": "./dist/bin.js",
  "files": [
    "dist",
    "templates",
    "src/prompts",
    "src/examples"
  ],
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "build": "node --experimental-strip-types scripts/copy-template.ts && esbuild src/bin.ts --bundle --platform=node --format=esm --target=node20 --external:esbuild --external:reveal.js --splitting --outdir=dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "generate:agents-md": "tsx scripts/generate-contract.ts"
  },
  "devDependencies": {
    "@decklee/core": "workspace:*",
    "@decklee/schema": "workspace:*",
    "@decklee/design-system": "workspace:*",
    "@types/node": "^22",
    "esbuild": "^0.21.5",
    "tsx": "^4.7.0",
    "typescript": "^5",
    "vitest": "^3"
  }
}
