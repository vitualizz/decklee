/**
 * Message rendered inside a <noscript> by @decklee/core when JavaScript is
 * disabled. The viewer is a client-side runtime, so the slides cannot render
 * without JS; this constant is the single source of truth for that copy.
 */
export const NO_JS_MESSAGE = "Enable JavaScript to view this presentation.";
